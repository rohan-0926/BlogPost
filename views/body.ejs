<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blogs</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/overlayscrollbars/1.13.0/css/OverlayScrollbars.min.css">
  <style>
    body {
        background-color: #222; /* Dark gray background color */
        font-family: Arial, sans-serif;
        display: flex; /* Use flexbox to create the desired layout */
        align-items: flex-start; /* Align items at the top */
        justify-content: space-between; /* Distribute content along the available space */
        min-height: 100vh; /* Make sure the content fills the whole viewport */
        margin: 0;
    }

    .main-container {
      display: flex;
      flex: 1;
      height: 100vh;
      overflow: hidden; /* Hide overflow to prevent double scrollbars */
    }


    .apform {
        background-color: #111; /* Slightly darker background color */
        padding: 40px;
        margin:20px auto;
        border-radius: 8px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2); /* Matching box shadow */
       /* Take up remaining horizontal space */
        flex: 1;
        display: flex;
       margin-top: 80px;
        align-items: center;
        overflow-y: auto;
    }

    ul {
        list-style-type: none;
        padding: 0;
    }

    li {
        margin-bottom: 20px;
        padding: 30px;
        background-color: #1c1c1c; /* Dark gray background color for list items */
        border-radius: 8px;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Matching box shadow for list items */
    }

    li a{
      text-decoration: none;
    }

    h2 {
        font-size: 24px;
        color: #fff;
        margin-bottom: 10px;
    }

    p {
        font-size: 16px;
        color: #ccc;
    }

    .pagination {
        margin-top: 20px;
    }

    .pagination a {
        color: #fff;
        text-decoration: none;
        margin: 0 5px;
    }

    #add_post {
      margin-bottom: 10px;
    }

    #home {
      margin-left: 10px;
      vertical-align: middle;
      display: inline-block;
      text-decoration: none;
    }

    /* Additional styling for the layout changes */
    .content-container {
        flex: 1; /* Take up 2/3 of the available width */
        padding: 20px;
        width: 50%; /* Set a fixed width for the addpost section */
      background-color: #333;
      border-radius: 8px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
      color: #fff;
      position: sticky; /* Keep the addpost section fixed */
      top: 20px; /* Adjust as needed */
      height: calc(100vh - 40px); /* Calculate the available height */
      overflow-y: auto;
    }

    body::-webkit-scrollbar {
      width: 12px;
    }
    
    body::-webkit-scrollbar-track {
      background-color: #222; /* Dark gray background color */
    }
    
    body::-webkit-scrollbar-thumb {
      background-color: #444; /* Slightly lighter gray color */
      border-radius: 8px;
    }
    
    body::-webkit-scrollbar-thumb:hover {
      background-color: #666; /* Even lighter gray color on hover */
    }
    

    /* Addpost.ejs content styles */
.create-post-container {
  background-color: #333;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  padding: 100px;
  max-width: 600px;
  width: 60%; /* Adjust the width */
  text-align: center;
  margin-left: 100px;
  margin-right: 100px;
  height: 80%; /* Adjust the height to fill the container */
  display: flex; /* Use flex to align content */
  flex-direction: column; /* Stack content vertically */
  justify-content: center; /* Center content vertically */
}

/* Form styles */
form {
  text-align: center; /* Center form elements horizontally */
  margin-top: 40px; /* Add some spacing */
}

.form-group {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 15px;
}

.form-group label {
  font-weight: bold;
  color: #eee;
  text-align: center;
  width: 100%;
  margin-bottom: 5px; /* Add spacing between label and input */
}

.form-group input[type="text"],
.form-group textarea {
  width: 80%;
  padding: 8px;
  border: 1px solid #ccc;
  border-radius: 1px;
  background-color: #fff;
  color: #333;
  outline: none;
}

.form-group textarea {
  height: 120px;
  text-decoration: none;
  border: none;
}

button[type="submit"] {
  background-color: #007bff;
  color: #fff;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
  transition: background-color 0.3s ease-in-out, color 0.3s ease-in-out;
}

button[type="submit"]:hover {
  background-color: #0056b3;
}

.home-button {
  background-color: #1e1d1d;
  color: #fff;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
  margin-left: 10px;
  vertical-align: middle;
  text-decoration: none;
}

.home-button:hover {
  background-color: #555;
}

#date{
  float: right;
}
    
  </style>
</head>
<body>
  <div class="main-container">
    <form action="/addpost" method="post">
       
      <div class="apform">
        <!-- Content of the addpost.ejs file -->
        <div class="create-post-container">
            <h1 class="create-post-title">Create a New Post</h1>
                <div class="form-group">
                    <label for="title">Title:</label>
                    <input type="text" id="title" name="title" required>
                </div>
                <div class="form-group">
                    <label for="content">Content:</label>
                    <textarea id="content" name="content" required></textarea>
                </div>
                <button type="submit">Create Post</button>
        </div>
      </div>
    </form>
   
    
    <div class="content-container">
      <!-- Content of the body.ejs file -->
      <ul>
        <% for (let i = startIndex; i < endIndex; i++) { %>
          <li>
            <!-- <br> -->
            <!-- <p id="date"><%= new Date(posts[i].createdAt).toLocaleString() %></p> -->
            <a href="/post/<%= posts[i].id %>"><h2><%= posts[i].title %></h2></a>

            <p><%= posts[i].content %></p>
            
          </li>
        <% } %>
      </ul>
    
      <!-- Pagination controls -->
      <div class="pagination">
        <% if (startIndex > 0) { %>
          <a href="/?page=<%= currentPage - 1 %>">Previous</a>
        <% } %>
        <% if (endIndex < posts.length) { %>
          <a href="/?page=<%= currentPage + 1 %>">Next</a>
        <% } %>
      </div>
      <br>
      
      <a href="/" class="home-button" id="home">Home</a>
    </div>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/overlayscrollbars/1.13.0/js/OverlayScrollbars.min.js"></script>
  <script>
    // Initialize OverlayScrollbars on the content container
    OverlayScrollbars(document.getElementById('custom-scrollbar'), {});
  </script>
  
</body>
</html>
